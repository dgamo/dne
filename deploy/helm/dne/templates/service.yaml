apiVersion: v1
kind: Service
metadata:
  name: {{ include "dne.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "dne.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: metrics
      port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
  selector:
    {{- include "dne.selectorLabels" . | nindent 4 }}
