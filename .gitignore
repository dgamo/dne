# Binaries — tools and the compiled controller live under bin/.
# Don't add a bare "dne" pattern: it would also match cmd/dne/ and
# deploy/helm/dne/ (which are real source dirs that must be tracked).
bin/

# Test and build outputs
coverage.out
coverage.html
dist/

# IDE
.idea/
.vscode/

# OS
.DS_Store
